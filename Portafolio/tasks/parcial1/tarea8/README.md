# Tarea 8 - Stored Procedures avanzados + Setup de Flask en Linux

## Archivos
- `tarea8.html`: reporte principal
- `17-feb.sql`: script SQL y comandos documentados
- `images/`: evidencias (`img1.png`, `img2.png`, `img3.png`)

## Alcance documentado
1. Revisión de `Alumnos` en `Biblioteca`.
2. Procedure `buscarAlumnos` con lista CSV y `FIND_IN_SET`.
3. Procedure `contarAlumnos` con parámetro `OUT` y lectura con `@s`.
4. Preparación de entorno Python (`python3-pip`) e instalación de Flask.

> Nota: se documenta solo lo respaldado por evidencia disponible.
